# Production requirements (without SpaCy - uses preprocessed files)
Flask==2.3.3
Flask-CORS==4.0.0
gunicorn>=21.0.0

# For development/testing only
# SpaCy is only needed for preprocessing words locally
# In production, we use preprocessed word files
